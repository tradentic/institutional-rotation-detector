# @airnub/resilient-http-policies

Reference implementation of the resilient HTTP policy engine per the v0.3 spec. It provides in-memory engines plus HTTP interceptors that plug into `@airnub/resilient-http-core`.
