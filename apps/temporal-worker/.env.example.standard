# ============================================================================
# STANDARD TIER CONFIGURATION
# ============================================================================
# Use case: Small fund, serious trader, production deployment
# Monthly cost: ~$500-800
# Workflows: 14 production workflows
# Features: Core + options flow + Form 4 + graph analysis
#
# See: docs/architecture/DEPLOYMENT_TIERS.md
# ============================================================================

# Deployment Tier
DEPLOYMENT_TIER=standard

# Feature Flags (Standard tier defaults)
ENABLE_OPTIONS_FLOW=true
ENABLE_FORM4_TRACKING=true
ENABLE_GRAPH_ANALYSIS=true
ENABLE_MICROSTRUCTURE=false          # Still disabled (unproven alpha)
ENABLE_RESEARCH_WORKFLOWS=false      # Research tools not needed

# Options Configuration
# 'minimal' = 3 API calls (contracts, flow, alerts)
# 'standard' = Default behavior
# 'deep' = Full Greeks + GEX (requires Advanced tier)
OPTIONS_TIER=minimal

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Supabase (Database)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# SEC EDGAR Configuration
# IMPORTANT: Replace with your actual name and email
SEC_USER_AGENT=YourCompany contact@yourcompany.com
EDGAR_BASE=https://www.sec.gov
EDGAR_DATA_API_BASE=https://data.sec.gov

# OpenAI API (for AI-powered analysis)
OPENAI_API_KEY=sk-your-openai-key-here

# UnusualWhales API (for options flow)
# Required in Standard tier for options overlay in R-score
# Get API key from: https://unusualwhales.com/
UNUSUALWHALES_API_KEY=your-unusualwhales-api-key-here

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_ENABLE_CACHING=true

# Temporal Configuration
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=rotation-detector
TEMPORAL_ADDRESS=localhost:7233

# ETF Tracking
ISHARES_FUNDS=IWB,IWM,IWN,IWC

# Rate Limiting
MAX_RPS_EDGAR=8
QUARTER_BATCH=8

# ============================================================================
# NOT REQUIRED FOR STANDARD TIER
# ============================================================================

# The following API keys are NOT needed in Standard tier:
# - FINRA_API_CLIENT / FINRA_API_SECRET (microstructure disabled)
# - IEX_API_KEY (microstructure disabled)

# These are only required for Advanced tier with microstructure enabled.

# ============================================================================
# ESTIMATED COSTS (Standard Tier)
# ============================================================================
# Base (from Minimal): ~$156/mo
# + UnusualWhales API: ~$100/mo (Tier 1 endpoints only)
# + Form 4 ingestion: Free (SEC EDGAR)
# + Graph compute: ~$50/mo (weekly rebuilds)
# + Increased DB usage: ~$20/mo (+200K rows)
# SUBTOTAL: ~$326/mo
#
# With realistic scaling:
# - More tickers (50 → 200): +$200 API calls
# - More workers (2 instances): +$140
# Realistic range: $500-800/mo
#
# ============================================================================
# EXPECTED PERFORMANCE IMPROVEMENT (vs. Minimal)
# ============================================================================
# - Hit rate: 60-70% → 70-75% (options filtering)
# - Sharpe ratio: 0.8-1.2 → 1.0-1.4 (Form 4 validation)
# - False positive reduction: ~30% (options + Form 4)
# - Early signals: 43 days faster than 13F (via Form 4)
